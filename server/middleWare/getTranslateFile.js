// 获取中英文切换的数据并生成文件
async function getTranslateFile(ctx, next) {
  console.log('9999999999999');
  let p = await Promise.resolve(1);
  // getData ()
  // await next()
  console.log(p);
}
getTranslateFile();
